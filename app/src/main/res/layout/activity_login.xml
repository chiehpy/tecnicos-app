<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true"
    android:background="@color/gray_background">

    <LinearLayout
        android:orientation="vertical"
        android:padding="32dp"
        android:gravity="center_horizontal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <!-- Logo -->
        <ImageView
            android:layout_width="240dp"
            android:layout_height="wrap_content"
            android:adjustViewBounds="true"
            android:layout_marginTop="48dp"
            android:layout_marginBottom="32dp"
            android:src="@drawable/logo_enofir"
            android:contentDescription="Logo Grupo Enofir" />

        <!-- Título -->
        <TextView
            android:text="Técnicos App"
            android:textSize="24sp"
            android:textColor="@color/enofir_primary"
            android:textStyle="bold"
            android:layout_marginBottom="32dp"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"/>

        <!-- Usuario -->
        <EditText
            android:id="@+id/etUser"
            android:hint="Usuario"
            android:inputType="text"
            android:background="@android:drawable/edit_text"
            android:padding="12dp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"/>

        <!-- PIN -->
        <EditText
            android:id="@+id/etPass"
            android:hint="PIN (4 dígitos)"
            android:inputType="numberPassword"
            android:maxLength="4"
            android:background="@android:drawable/edit_text"
            android:padding="12dp"
            android:layout_marginTop="16dp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"/>

        <!-- Botón Ingresar -->
        <Button
            android:id="@+id/btnLogin"
            android:text="Ingresar"
            android:textColor="@android:color/white"
            android:backgroundTint="@color/enofir_primary"
            android:layout_marginTop="24dp"
            android:paddingVertical="12dp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"/>

        <!-- Mensaje de error/estado -->
        <TextView
            android:id="@+id/tvLoginMsg"
            android:text=""
            android:textColor="@color/error_red"
            android:gravity="center"
            android:layout_marginTop="16dp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"/>

    </LinearLayout>

</ScrollView>
